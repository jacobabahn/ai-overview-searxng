[build-system]
requires = ["setuptools>=77"]
build-backend = "setuptools.build_meta"

[project]
name = "searxng-ai-overview"
version = "0.1.0"
description = "Streaming, source-linked AI overviews for SearXNG"
readme = "README.md"
license = "MIT"
license-files = ["LICENSE", "src/ai_overview/static/*.LICENSE.txt"]
requires-python = ">=3.11"
dependencies = ["Flask>=3.1", "itsdangerous>=2.1", "PyYAML>=6"]

[project.urls]
Repository = "https://github.com/jacobabahn/ai-overview-searxng"
Issues = "https://github.com/jacobabahn/ai-overview-searxng/issues"

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
ai_overview = ["static/*", "templates/*"]

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E4", "E7", "E9", "F", "I", "UP", "B", "ANN"]
# Preserve the project's explicit typing.Optional convention.
ignore = ["ANN401", "UP045"]

[dependency-groups]
dev = [
    "httpx>=0.27",
    "pytest>=8",
    "ruff>=0.9",
    "playwright>=1.49",
    "ty>=0.0.82",
]
